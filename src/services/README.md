# Services

API clients and data-fetching layer. TanStack Query `queryOptions` factories
and typed wrappers around `createServerFn` calls live here.

Server-only files should end in `.server.ts`; client-callable server
functions should end in `.functions.ts`.