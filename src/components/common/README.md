# Common components

Reusable, presentation-only building blocks shared across pages
(buttons, cards, layout primitives). Keep them stateless where possible.

Shadcn primitives live in `src/components/ui/`.