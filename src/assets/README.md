# Assets

Images, fonts, and other static assets imported via ES modules
(`import logo from "@/assets/logo.webp"`).

Prefer **WebP** for photos and **SVG** for icons/illustrations.
Use `public/` only for files that must be referenced by absolute URL
(favicon, robots.txt, manifest).